[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "misis-discrete-course-work"
version = "0.1.0"
dependencies = []

[tool.setuptools.packages.find]
include = ["backend*"]
exclude = ["research*", "tests*"]